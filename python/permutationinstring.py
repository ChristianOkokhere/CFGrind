def work(s1, s2):
    check = set(s1)
    cur = "".join(sorted([x for x in s1]))

    for i in range(len(s2)):
        
        if s2[i] in check and s2[i:i + len(s1)]:

    return False
    #for the rest of this what I am thinking about doing is just
    